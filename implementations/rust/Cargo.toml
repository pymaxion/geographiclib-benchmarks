[package]
name = "geographiclib-benchmark"
version = "0.1.0"
edition = "2021"

[dependencies]
geographiclib-rs = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
statrs = "0.17"

[profile.release]
opt-level = 3
lto = true
